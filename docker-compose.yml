services:
  app:
    container_name: kanban-mvp-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - kanban_data:/app/backend/data

volumes:
  kanban_data:
